import type { NextPage } from "next";
import Layout from "../layout/Layout";

const ProgressionPage: NextPage = () => {
    return (
        <Layout>
            <div>
                Progression Page
            </div>
        </Layout>
    );
};

export default ProgressionPage;
